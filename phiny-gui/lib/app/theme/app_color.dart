import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF1E88E5); // Blue for primary actions
  static const primaryLight = Color(0xFFBBDEFB); // Light blue for backgrounds

  // Background colors
  static const background = Color(0xFFF5F8FA);
  static const white = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF657786);
  static const textTertiary = Color(0xFF8899A6);

  // Avatar colors
  static const avatarBg = Color(0xFFBBDEFB); // Light blue avatar background
  static const avatarIcon = Color(0xFF1E88E5); // Blue avatar icon

  // Button colors
  static const buttonBg = Color(0xFFE3F2FD); // Light blue button background
  static const buttonBgMuted = Color(0xFFBBDEFB); // Muted button background

  // Action button colors
  static const red = Color(0xFFE53935); // Red for end call button
  static const redLight = Color(0xFFFFEBEE);
  // Border and divider colors
  static const border = Color(0xFFE1E8ED);
  static const divider = Color(0xFFEEEEEE);

  // Sidebar colors
  static const sidebarBg = Color(0xFFFFFFFF);
  static const sidebarIconActive = Color(0xFF1E88E5);
  static const sidebarIconInactive = Color(0xFF657786);
  static const sidebarItemBg = Color(0xFFE3F2FD); // Light blue for active item
  static const sidebarItemBgHover = Color(0xFFF5F5F5);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFE53935);
  static const info = Color(0xFF1E88E5);

  // Call screen specific
  static const callTimerText = Color(0xFF657786);

  // Opacity values
  static const double iconOpacity = 0.87;
  static const double disabledOpacity = 0.38;
}
